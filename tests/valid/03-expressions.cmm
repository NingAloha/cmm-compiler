int main()
{
    int a, b, c;
    a = b + c * 2;
    a = -a + !(b < c && c != 0 || a == 1);
    if (a > 0)
        a = a - 1;
    else
        a = a + 1;
    while (a < 10)
    {
        a = a + 1;
    }
    return a;
}
